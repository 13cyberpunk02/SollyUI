namespace Solly.UI.Core;

/// <summary>Built-in accent palettes. Each is just an HSL hue.</summary>
public enum SPalette
{
    /// <summary>Neon teal — the default.</summary>
    Teal,
    Cyan,
    Violet,
    Magenta,
    Amber,
    Lime,
    Custom
}